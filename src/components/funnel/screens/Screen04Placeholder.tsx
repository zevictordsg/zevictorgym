"use client";

import { ScreenPlaceholder } from "./ScreenPlaceholder";
import type { FunnelScreenProps } from "../types";

export function Screen04Placeholder(props: FunnelScreenProps) {
  return (
    <ScreenPlaceholder
      {...props}
      title="Comparação de modelos"
      description="Cutting agressivo / Carbmaxxing hack / Bulking. Preciso do link do Figma dessa tela (a quarta) pra implementar com fidelidade."
    />
  );
}
